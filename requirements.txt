# Lambda deployment dependencies
# Package with: pip install -r requirements.txt -t ./package
#
# Note: boto3 is included in the Lambda runtime by default, but pinning it
# here keeps local development and packaging reproducible.
psycopg2-binary==2.9.10
requests==2.32.3
boto3==1.35.99
